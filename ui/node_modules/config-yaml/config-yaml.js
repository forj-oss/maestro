module.exports = {
  get_config_yaml: function(){
    var yaml = require('js-yaml');
    var fs   = require('fs');
    console.log(__dirname);
    var config = yaml.safeLoad(fs.readFileSync('./config/config.yaml', 'utf8'));
    return config;
  }
}