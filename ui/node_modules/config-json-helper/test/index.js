var should = require('chai').should();
var configJsonHelper = require('../index');

describe('#get', function() {
  it('reads a valid key from config.json', function() {
    configJsonHelper.get('site:blueprint').should.equal('redstone');
  });
});