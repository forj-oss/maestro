module.exports = {
  get_tools: function(raw_json){
    var tools = [];
    JSON.parse(raw_json).site.node.forEach(function(node){
      node.tools.forEach(function(tool){
        tools.push(tool);
      });
    });
    return tools;
  }
}